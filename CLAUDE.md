# Slotera

Project documentation lives in vendor-neutral files so that every assistant reads the same
content. This file exists only to point there.

**Read [`AGENTS.md`](AGENTS.md) first.** It is the entry point: current state, repository
layout, how to run the project, conventions, the reading order for the other documents,
and the index of workflow modules in [`skills/`](skills/).

The product rulebook that used to live in this file is now
[`docs/PRODUCT.md`](docs/PRODUCT.md) — same content, neutral path.
